const swiper = new Swiper('.swiper', {
  // autoplay {
  //   delay:3000,
  // },
  loop: true,

  // If we need pagination
  pagination: {
    el: '.paginations',
    clickable : true,
  },


  // And if we need scrollbar
  scrollbar: {
    el: '.swiper-scrollbar',
  },


});


